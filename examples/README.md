# examples — ejemplos compilados

Un ejemplo compilado (`.tex` + `.pdf`) por clase/tipo, como documentación viva y
"test" visual del sistema. Se generan compilando las plantillas de `templates/`
con `scripts/build.sh`.
